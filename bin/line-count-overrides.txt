# Line-count override file for bin/line-count-ratchet.sh
# Each line: <relative-path> <new-baseline>
# Raise a baseline only after deliberate review of why the file grew.

# NXT-586: Tier 2 of YAML layout parity — wired the operator-driven YAML
# reload key by adding a single colocated helper
# (`%chat-handle-yaml-reload-key!`) to yaml-theme-layout.lisp. The helper
# keeps reload-related code in one place per the file's existing
# section-comment structure rather than splitting across files. New
# baseline = 255 (current) + 25 headroom.
#
# NXT-587: Tier 3 of YAML layout parity — added the inline file watcher
# (`%yaml-theme-poll-and-publish-if-changed`) plus a `:trigger` keyword
# threaded through `%chat-handle-yaml-reload-key!` so operator vs watcher
# reloads emit distinct toasts/log lines. New baseline = 299 (current) + 25.
#
# NXT-597: NXT-593 (decision memo, 2026-04-29) reversed the earlier
# "no generic primitive" stance once the third call site was confirmed.
# The inline watcher was extracted into `amoebum/src/fp/file-watcher.lisp`
# and yaml-theme-layout now owns only a small lazy-cache helper plus a
# `poll-watcher-once` delegation. Net file delta is +12 (current = 311)
# because the cache machinery (struct-validity check, lazy init, mtime
# seeding) replaced a bare inline `publish` call. Baseline stays at 324
# — well above current 311 — to leave headroom for follow-up tightening
# rather than churn the override now.
amoebum/src/ui/yaml-theme-layout.lisp 324

# NXT-588: Wired YAML `padding`, `focusable`, and `focus-order` through the
# chat-panel by reusing PTUI's existing primitives — make-box-widget for
# padding (ptui/src/widgets/core.lisp:88) and runtime focus-cycle for
# focusable propagation (ptui/src/ui/runtime.lisp:295). Added four
# resolvers (%chat-panel-padding-spec, %chat-panel-region-focusable-p,
# %chat-panel-region-focus-order, %chat-panel-region-id), one wrap helper
# (%chat-panel-wrap-padding), one focus re-tagger (%chat-panel-mark-focusable),
# and applied %chat-panel-wrap-padding around each layout child. Net delta
# is ~73 lines (343 -> 416). Baseline = 440 to leave 25 lines of headroom
# for follow-up tightening (the wrap-padding cond could fold once we
# extract the box-with-focus combination into a single helper).
amoebum/src/ui/panels/chat-panel.lisp 440
